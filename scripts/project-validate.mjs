import fs from'node:fs';const p=JSON.parse(fs.readFileSync('docs/PROJECT-PLAN.json')),ids=[...p.milestones,...p.checkpoints,...p.tasks].map(x=>x.id);if(new Set(ids).size!==ids.length)throw Error('Duplicate plan IDs');for(const t of p.tasks)if(!['PLANNED','READY','IN_PROGRESS','BLOCKED','DONE','ACCEPTED','CANCELLED'].includes(t.status))throw Error(`Invalid status ${t.id}`);console.log('Project plan valid');
