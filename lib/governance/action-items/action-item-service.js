import { db as defaultDb } from '../../db'; import { fail } from '../errors'; import { recordGovernanceEvent } from '../activity/governance-audit-log';
export async function createActionItem({findingId=null,refinementFindingId=null,ownerId,title,dueDate,actor,manualSource,reason,db=defaultDb}) { if(findingId&&refinementFindingId)fail('INVALID_ACTION_SOURCE','An action item may have only one finding source.'); if(!findingId&&!refinementFindingId&&(!manualSource||!reason))fail('MISSING_REQUIRED_METADATA','Manual action requires manual source and reason.'); if(!ownerId||!title)fail('MISSING_REQUIRED_METADATA','Owner and title are required.'); const action=await db.actionItem.create({data:{findingId,refinementFindingId,ownerId,title,dueDate:dueDate||null}}); await recordGovernanceEvent(db,{actor,businessUnitId:null,entity:'ActionItem',entityId:action.id,action:'ACTION_ITEM_CREATED',reason,metadata:{findingId,refinementFindingId,manualSource}});return action; }
