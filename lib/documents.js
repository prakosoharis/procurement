import { db } from './db';

export const allowedDocumentTypes = new Set(['application/pdf','application/vnd.openxmlformats-officedocument.wordprocessingml.document']);
export function canManageBusinessUnit(user, businessUnitId) { return user && ['COMPLIANCE_ADMIN','CORPORATE_PROCUREMENT'].includes(user.role); }
export function nextVersion(version) { const match = /^v(\d+)\.(\d+)$/i.exec(version || 'v0.0'); return match ? `v${match[1]}.${Number(match[2]) + 1}` : 'v1.0'; }
export async function writeAudit(actorId, entity, entityId, action, detail) { return db.auditLog.create({data:{actorId,entity,entityId,action,detail}}); }
export function documentDto(document) { const versions=document.versions||[],version=versions[0]; return { id:document.id, title:document.title, status:document.status, language:document.language, businessUnit:document.businessUnit, documentType:document.documentType, owner:document.owner || null, currentVersion:version?.versionNo || document.currentVersion, fileName:version?.fileName || null, fileKey:version?.fileKey || null, contentType:version?.contentType || null, uploadedAt:version?.uploadedAt || document.updatedAt, versionId:version?.id || null, versionHistory:versions.map(v=>({id:v.id,versionNo:v.versionNo,fileName:v.fileName,changeSummary:v.changeSummary,approvalStatus:v.approvalStatus,uploadedAt:v.uploadedAt,approvedAt:v.approvedAt,approvedBy:v.approvedBy||null})) }; }
