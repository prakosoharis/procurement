# M2-C1 Request Workflow Reconciliation

Status: DONE. Existing legacy routes use `SopRequest` with `SUBMITTED`, `IN_REVIEW`, `REVISION_REQUIRED`, `APPROVED`, and `REJECTED`; native M2 must retain these semantics and coexist without changing lifecycle status behavior.
