'use client';import{useEffect,useState}from'react';import{useRouter}from'next/navigation';
export default function CreateSop(){const router=useRouter(),[o,setO]=useState(null),[message,setMessage]=useState('');useEffect(()=>{fetch('/api/governance/options').then(r=>r.json()).then(x=>setO(x.data))},[]);const submit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),body={title:f.get('title'),businessUnitId:f.get('businessUnitId'),categoryId:f.get('categoryId')||undefined,ownerId:f.get('ownerId')||undefined,initialVersionNumber:f.get('initialVersionNumber')};const r=await fetch('/api/governance/sops',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),x=await r.json();if(!x.ok)return setMessage(x.error?.message||'Unable to create SOP.');router.push(`/sop-governance/repository/${x.data.sopDocumentId}`)};if(!o)return <main className="native-page">Loading form…</main>;return <main className="native-page"><h1>Create SOP</h1><p>Metadata-first creation. Attach the controlled document separately.</p><form onSubmit={submit} className="repository-card"><label>Title<input required name="title"/></label><label>Business Unit<select required name="businessUnitId"><option value="">Select</option>{o.businessUnits.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>Category<select name="categoryId"><option value="">Uncategorized</option>{o.categories.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>Owner<select name="ownerId"><option value="">Unassigned</option>{o.owners.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select></label><label>Initial version<input required name="initialVersionNumber" defaultValue="1.0"/></label><button type="submit">Create Draft SOP</button><p>{message}</p></form></main>}
