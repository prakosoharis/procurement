'use client';import{useEffect,useState}from'react';
export default function Requests(){const[r,setR]=useState(null),[show,setShow]=useState(false),[message,setMessage]=useState('');const load=()=>fetch('/api/governance/requests').then(x=>x.json()).then(x=>setR(x.data||[]));useEffect(load,[]);const submit=async e=>{e.preventDefault();const f=new FormData(e.currentTarget),b=Object.fromEntries(f);const x=await fetch('/api/governance/requests',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(b)}).then(x=>x.json());if(!x.ok)return setMessage(x.error?.message);setShow(false);load()};return <main className="native-page"><h1>SOP Requests</h1><p>Native request intake view. Existing legacy request workflow remains unchanged.</p><button onClick={()=>setShow(!show)}>Create Request</button>{show&&<form className="repository-card" onSubmit={submit}><input name="title" placeholder="Request title" required/><input name="sopDocumentId" placeholder="SOP ID" required/><input name="changeType" placeholder="Change type" required/><input name="clauseReference" placeholder="Clause reference" required/><textarea name="description" placeholder="Business context" required/><textarea name="proposedText" placeholder="Proposed change" required/><button>Submit Request</button><p>{message}</p></form>}<section className="repository-card"><table><thead><tr><th>Request</th><th>SOP</th><th>Status</th><th>Requester</th></tr></thead><tbody>{!r?<tr><td colSpan="4">Loading…</td></tr>:r.map(x=><tr key={x.requestId}><td>{x.title}</td><td>{x.sop?.title||'—'}</td><td>{x.status}</td><td>{x.requester?.name}</td></tr>)}</tbody></table></section></main>}
