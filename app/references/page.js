'use client'; import { useEffect,useState } from 'react';
export default function References(){const [rows,setRows]=useState([]),[msg,setMsg]=useState('');async function load(){setRows(await (await fetch('/api/references')).json())}useEffect(()=>{load()},[]);async function submit(e){e.preventDefault();const r=await fetch('/api/references',{method:'POST',body:new FormData(e.currentTarget)});const b=await r.json();setMsg(r.ok?'Reference tersimpan.':b.error||'Gagal');if(r.ok){e.currentTarget.reset();load()}}return <main className="shell grid two"><section><h1>Reference Center</h1><p className="notice">MVP mendukung manual upload atau URL resmi. Tidak ada auto-scraping regulasi.</p><form className="card form" onSubmit={submit}><div><label>Judul reference</label><input name="title" required/></div><div><label>Tipe</label><select name="type"><option>Regulation</option><option>Audit Finding</option><option>Best Practice</option><option>Internal Policy</option></select></div><div><label>URL resmi (opsional)</label><input name="sourceUrl" type="url"/></div><div><label>File (opsional)</label><input name="file" type="file" accept=".pdf,.docx"/></div><button className="button">Simpan Reference</button>{msg&&<p>{msg}</p>}</form></section><section className="card"><h3>Reference tersedia</h3>{rows.map(r=><div key={r.id} style={{padding:'12px 0',borderBottom:'1px solid var(--line)'}}><b>{r.title}</b><br/><small className="muted">{r.type}</small>{r.sourceUrl&&<><br/><a href={r.sourceUrl} target="_blank">Buka URL resmi</a></>}{r.fileKey&&<><br/><a href={`/api/files/download?key=${encodeURIComponent(r.fileKey)}`}>Download file</a></>}</div>)}</section></main>}
