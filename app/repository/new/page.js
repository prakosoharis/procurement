'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function NewSop(){const [message,setMessage]=useState('');const router=useRouter();async function submit(e){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch('/api/sops',{method:'POST',body:f});const body=await r.json();if(!r.ok)return setMessage(body.error||'Gagal menyimpan');router.push(`/repository/${body.id}`);router.refresh()}return <main className="shell"><h1>Upload / Buat SOP</h1><p className="notice">File akan disimpan privat di MinIO. Endpoint S3 dapat diganti lewat environment saat staging/production.</p><form className="card form" onSubmit={submit}><div><label>Judul SOP</label><input name="title" required/></div><div><label>Business Unit</label><input name="businessUnit" defaultValue="BU Logistik A" required/></div><div className="grid two"><div><label>Bahasa</label><select name="language"><option value="id">Bahasa Indonesia</option><option value="en">English</option></select></div><div><label>Versi</label><input name="version" defaultValue="v1.0" required/></div></div><div><label>Dokumen SOP (PDF/DOCX)</label><input name="file" type="file" accept=".pdf,.docx"/></div><button className="button">Simpan sebagai Draft</button>{message&&<p>{message}</p>}</form></main>}
