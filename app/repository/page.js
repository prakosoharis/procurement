import Link from 'next/link'; import { db } from '../../lib/db';
export const dynamic = 'force-dynamic';
export default async function Repository(){const docs=await db.sopDocument.findMany({include:{businessUnit:true,versions:{orderBy:{uploadedAt:'desc'},take:1}},orderBy:{updatedAt:'desc'}});return <main className="shell grid"><div className="actions" style={{justifyContent:'space-between'}}><div><h1>SOP Repository</h1><p className="muted">Dokumen resmi, metadata, versi, dan status publishing.</p></div><Link className="button" href="/repository/new">+ Upload / Buat SOP</Link></div><section className="card"><table><thead><tr><th>SOP</th><th>Business Unit</th><th>Versi</th><th>Status</th><th>File</th></tr></thead><tbody>{docs.map(d=><tr key={d.id}><td><Link href={`/repository/${d.id}`}><b>{d.title}</b></Link><br/><small className="muted">{d.language.toUpperCase()}</small></td><td>{d.businessUnit.name}</td><td>{d.currentVersion||'—'}</td><td><span className="badge">{d.status}</span></td><td>{d.versions[0]?.fileKey?<a className="button secondary" href={`/api/files/download?key=${encodeURIComponent(d.versions[0].fileKey)}`}>Download</a>:'Belum ada file'}</td></tr>)}</tbody></table>{!docs.length&&<p className="empty">Belum ada SOP.</p>}</section></main>}
