import { NextResponse } from 'next/server'; import { db } from '../../../lib/db'; import { uploadObject } from '../../../lib/storage'; import { canManageReferences,currentUser } from '../../../lib/current-user';
export async function GET(){return NextResponse.json(await db.referenceSource.findMany({orderBy:{createdAt:'desc'}}))}
export async function POST(request){try{const user=await currentUser();if(!canManageReferences(user))return NextResponse.json({error:'Compliance access required'},{status:403});const data=await request.formData(),title=data.get('title')?.toString().trim(),type=data.get('type')?.toString(),sourceUrl=data.get('sourceUrl')?.toString()||null,file=data.get('file');if(!title||!type)return NextResponse.json({error:'Judul dan tipe wajib diisi.'},{status:400});let fileKey=null;if(file&&file.size){fileKey=`references/${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'_')}`;await uploadObject({key:fileKey,body:Buffer.from(await file.arrayBuffer()),contentType:file.type||'application/octet-stream'})}const row=await db.referenceSource.create({data:{title,type,sourceUrl,fileKey}});return NextResponse.json(row,{status:201})}catch(error){console.error(error);return NextResponse.json({error:'Simpan reference gagal.'},{status:500})}}
