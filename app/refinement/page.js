'use client'; import { Suspense, useState } from 'react'; import { useSearchParams } from 'next/navigation';
function RefinementForm(){const q=useSearchParams(),[requestId,setRequestId]=useState(q.get('request')||''),[result,setResult]=useState(null),[error,setError]=useState('');async function run(e){e.preventDefault();setError('');const r=await fetch('/api/refinement',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({requestId})});const b=await r.json();if(!r.ok)return setError(b.error||'Gagal');setResult(b)}return <main className="shell grid"><div><h1>SOP Refinement</h1><p className="muted">Analisis menghasilkan temuan kandidat yang harus divalidasi manusia.</p></div><form className="card form" onSubmit={run}><div><label>Request ID</label><input value={requestId} onChange={e=>setRequestId(e.target.value)} placeholder="Buka request terlebih dahulu lalu gunakan tombol Jalankan Refinement" required/></div><button className="button">Jalankan candidate analysis</button>{error&&<p className="badge high">{error}</p>}</form>{result&&<section className="card"><h3>Hasil kandidat</h3><div className="finding"><span className="badge high">{result.finding.risk}</span><h3>{result.finding.title}</h3><p><b>Evidence:</b> {result.finding.evidence}</p><p><b>Rekomendasi:</b> {result.finding.recommendation}</p><p className="notice">Temuan tersimpan sebagai PENDING dan menunggu Validation Queue.</p></div></section>}</main>}
export default function Refinement(){return <Suspense fallback={<main className="shell">Memuat refinement…</main>}><RefinementForm/></Suspense>}
