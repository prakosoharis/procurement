'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function Login(){const [error,setError]=useState('');const router=useRouter();async function submit(e){e.preventDefault();const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!res.ok)return setError('Email atau password tidak valid.');router.push('/');router.refresh()}return <><style jsx global>{`.top{display:none}`}</style><main className="shell"><section className="card" style={{maxWidth:460,margin:'40px auto'}}><h1>Masuk</h1><p className="muted">Masuk menggunakan akun yang telah diberikan oleh administrator.</p><form className="form" onSubmit={submit}><div><label>Email</label><input name="email" type="email" required/></div><div><label>Password</label><input name="password" type="password" required/></div>{error&&<p className="badge high">{error}</p>}<button className="button">Masuk</button></form></section></main></>}
