import Link from 'next/link'; import { db } from '../../lib/db';
export const dynamic = 'force-dynamic';
export default async function Requests(){const rows=await db.sopRequest.findMany({include:{requester:true,findings:true},orderBy:{updatedAt:'desc'}});return <main className="shell grid"><div className="actions" style={{justifyContent:'space-between'}}><div><h1>SOP Requests</h1><p className="muted">Request baru, exception, dan revisi SOP.</p></div><Link className="button" href="/requests/new">+ Buat Request</Link></div><section className="card"><table><thead><tr><th>Request</th><th>Pemohon</th><th>Status</th><th>Candidate finding</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><Link href={`/requests/${r.id}`}><b>{r.title}</b></Link><br/><small>{r.requestType}</small></td><td>{r.requester.name}</td><td><span className="badge pending">{r.status}</span></td><td>{r.findings.length}</td></tr>)}</tbody></table></section></main>}
