'use client'; import { useRouter } from 'next/navigation'; import { useState } from 'react';
export default function NewRequest(){const router=useRouter(),[error,setError]=useState('');async function submit(e){e.preventDefault();const r=await fetch('/api/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const b=await r.json();if(!r.ok)return setError(b.error||'Gagal');router.push(`/requests/${b.id}`);router.refresh()}return <main className="shell"><h1>Buat SOP Request</h1><p className="notice">Request akan masuk ke validation queue. AI refinement hanya membuat candidate finding, tidak mengubah status resmi.</p><form className="card form" onSubmit={submit}><div><label>Judul request</label><input name="title" placeholder="Contoh: Revisi SOP Tender Terbuka" required/></div><div><label>Jenis request</label><select name="requestType"><option value="NEW_SOP">SOP Baru</option><option value="REVISION">Revisi SOP</option><option value="EXCEPTION">Request Exception</option></select></div><div><label>Deskripsi dan konteks</label><textarea name="description" required placeholder="Jelaskan kebutuhan bisnis, referensi, dan dampaknya."/></div>{error&&<p className="badge high">{error}</p>}<button className="button">Submit Request</button></form></main>}
